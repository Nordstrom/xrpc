allprojects {
  repositories {
    mavenCentral()
    jcenter()
  }

  apply plugin: 'java'
  apply plugin: 'checkstyle'


  checkstyle {
    toolVersion '8.3'
    configFile = rootProject.file('config/checkstyle/checkstyle.xml')
  }
}
